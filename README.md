# calibre-cli-docker-interface

Lightweight dockerised interface for Calibre's CLI tools, intended for resource-constrained Linux hosts (NAS, mini-PCs). Browse, upload, refresh metadata, convert formats, and push books to a USB-attached MTP e-reader — all from a small FastAPI web app.

Full documentation lives under `docs/` (work in progress).
